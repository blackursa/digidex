async function getUserProfile(userId) {
  // Implementation will be mocked in tests
}

async function createContactRequest(userId) {
  // Implementation will be mocked in tests
}

module.exports = {
  getUserProfile,
  createContactRequest,
};
